import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette
  static const Color lightIceBlue = Color(0xFFE2F0FF);
  static const Color mediumBlue = Color(0xFF6385C3);
  static const Color primaryDarkBlue = Color(0xFF2A548D);
  static const Color deepSpaceNavy = Color(0xFF080F21);

  // Secondary Accent Palette
  static const Color vibrantTeal = Color(0xFF20C997);
  // static const Color electricBlue = Color(0xFF...); // Add hex code when available
  // static const Color electricCyan = Color(0xFF...); // Add hex code when available

  // Generic
  static const Color white = Color(0xFFFFFFFF);
  static const Color grey = Color(0xFF888888);
}